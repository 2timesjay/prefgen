# Image Prompt Tuning via hyperparameter optimization

Use hyperparameter optimization Optuna to tune prompts

## Installation

```sh
pyenv virtualenv opt-gen
```

Install reqs

```sh
pyenv activate opt-gen
pip install -r requirements.txt

```